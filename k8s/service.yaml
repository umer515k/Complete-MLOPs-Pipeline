apiVersion: v1
kind: Service
metadata:
  name: mlops-pipeline-service
spec:
  selector:
    app: mlops-pipeline
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30000
